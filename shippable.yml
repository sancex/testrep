language: php

php:
  - 7.2

services:
  - #any supported service

env:
  - #env1=foo
  - #env2=bar

matrix:

build:
  ci:
    - php a.php
    - composer require --dev phpunit/phpunit ^8.5
    - phpunit CiTest
    
  on_success:
    - echo 'success'
   
  on_failure:
    - echo 'wht' #command1
