language: php

php:
  - 7.3

services:
  - #any supported service

env:
  - #env1=foo
  - #env2=bar

matrix:

build:
  ci:
    - php a.php
    - #command2
  post_ci:
    - #command1
    - #command2
  on_success:
    - echo 'success'
   
  on_failure:
    - echo 'wht' #command1


integrations:
 notifications:
   - integrationName: github
     type: GitHub
     recipients:
       - #recp1
       - #recp2

  hub:
    - integrationName: github
      type: GitHub
      agent_only:
